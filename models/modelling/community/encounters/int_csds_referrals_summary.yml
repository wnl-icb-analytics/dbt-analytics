models:
  - name: int_csds_referrals_summary
    description: |
      Superseded by fct_csds_referral_summary and retiring once remaining readers move. Summary of the number of referrals per patient.
      One row per patient (NHS_NUMBER).
    config:
      meta:
        owner:
          name: Emily Baldwin
    columns:
      - name: sk_patient_id
        description: "Unique pseudo-anonymised patient identifier (NHS_NUMBER)"
        tests:
##          - not_null
          - unique
