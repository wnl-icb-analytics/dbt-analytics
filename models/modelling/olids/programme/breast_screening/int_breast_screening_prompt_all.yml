models:
  - name: int_breast_screening_prompt_all
    description: 'Determine whether a person has received a prompt for breast screening using ECL_CACHE Ref SMI_LONGER_LIVES_CANCER_SCREEN_PROMPTS cluster'
    config:
      meta:
        owner:
          name: KateHomer55
    data_tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: SMI_LONGER_LIVES_CANCER_SCREEN_PROMPTS
            source: ECL_CACHE
    # tests:
    #   - dbt_expectations.expect_table_row_count_to_be_between:
    #       arguments:
    #         min_value: 1

    columns:
      - name: person_id
        tests:
          - not_null
