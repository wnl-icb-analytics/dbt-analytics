models:
  - name: int_adult_imms_code_dose
    description: Intermediate table to match vaccines from the schedule with 
      code clusters and doses
    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          arguments:
            min_value: 1