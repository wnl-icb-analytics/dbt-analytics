version: 2
models:
  - name: stg_csds_referral_assessment
    description: One accepted referral assessment source occurrence. Each record is a question, dimension, subtotal or total completed outside a specific contact. Repeated equal scores on different dates remain separate.
    config:
      meta:
        owner:
          name: Eddie Davison
    columns:
      - name: cyp609_unique_id
        description: Source table row identifier within an accepted submission.
        data_tests: [not_null, unique]
      - name: unique_submission_id
        description: Accepted source submission supplying this occurrence.
