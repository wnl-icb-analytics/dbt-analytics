select
    clinical_record_id,
    source_record_type,
    source_record_id,
    person_id,
    patient_id,
    {{ consistent_sk_patient_id_format('sk_patient_id') }} as sk_patient_id,
    encounter_id,
    clinical_record_date,
    clinical_date_precision_code,
    clinical_date_precision_name,
    date_recorded,
    source_code,
    source_code_name,
    source_coding_system,
    mapped_code,
    mapped_code_name,
    mapped_coding_system,
    result_value,
    result_date,
    result_unit_source_code,
    result_unit_source_name,
    medication_name,
    provider_organisation_id,
    publisher_organisation_id,
    publisher_organisation_code,
    practitioner_id,
    source_extraction_date,
    provider_organisation_code,
    provider_code_authority,
    provider_organisation_name,
    publisher_organisation_name
from {{ ref('raw_olids_clinical_record') }}
