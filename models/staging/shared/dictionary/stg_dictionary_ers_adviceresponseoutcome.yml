models:
- name: stg_dictionary_ers_adviceresponseoutcome
  description: One supplied e-RS adviceresponseoutcome dictionary code with its current retained label.
    Includes all supplied codes without filtering historical values.
  config:
    meta:
      owner:
        name: Jonathan Linney
  columns:
  - name: code
    data_tests:
    - unique
    - not_null
  - name: display
    description: Current retained dictionary display; not a historical label snapshot.
