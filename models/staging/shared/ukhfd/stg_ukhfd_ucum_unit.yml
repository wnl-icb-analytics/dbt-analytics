version: 2
models:
  - name: stg_ukhfd_ucum_unit
    description: UKHFD UCUM expression history, one row per case-sensitive code and history start. Preserves definitions absent from the latest extract.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
