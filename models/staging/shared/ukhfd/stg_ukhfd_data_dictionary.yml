version: 2

models:
  - name: stg_ukhfd_data_dictionary_attendance_status
    description: UKHFD attendance-status definitions. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: &code_set_columns
      - name: source_code_set_name
        description: NHS Data Model and Dictionary attribute name.
        data_tests: [not_null]
      - name: code
        description: National code, upper-cased and trimmed.
        data_tests: [not_null]
      - name: description
        description: Published code description.
      - name: short_description
        description: Published description limited to 60 characters.
      - name: category
        description: Published category where supplied.
      - name: notes
        description: Published notes where supplied.
      - name: valid_from_date
        description: Date from which the definition is valid.
      - name: valid_to_date
        description: Last date on which the definition is valid; null when open-ended.
      - name: is_currently_valid
        description: TRUE when the code remains in the current published code set.
      - name: source_unique_key
        description: UKHFD key for the code definition.
      - name: source_imported_at
        description: Timestamp when UKHFD imported the source publication.
      - name: source_created_at
        description: Timestamp when UKHFD created the source row.
      - name: is_latest_definition
        description: TRUE for UKHFD's newest definition of the code, including retired codes.
      - name: source_effective_from_at
        description: Timestamp from which the UKHFD definition revision applies.
      - name: source_effective_to_at
        description: Timestamp until which the UKHFD definition revision applies; null for its newest revision.

  - name: stg_ukhfd_data_dictionary_activity_location_type
    description: UKHFD activity-location definitions. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_consultation_mechanism
    description: UKHFD consultation-mechanism definitions. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_mhsds_source_of_referral
    description: UKHFD MHSDS source-of-referral definitions. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_mental_health_source_of_referral_legacy
    description: UKHFD definitions for the retired mental-health source-of-referral code set. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_mhsds_service_or_team_type
    description: UKHFD mental-health service or team type definitions. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_csds_source_of_referral
    description: UKHFD community source-of-referral definitions. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_csds_service_or_team_type
    description: UKHFD community service or team type definitions. One row per code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_mhsds_care_contact_code_lookup
    description: >-
      UKHFD definitions for MHSDS care-contact code sets. One row per
      code set, code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - code_set_name
              - source_code_set_name
              - code
              - source_effective_from_at
    columns:
      - name: code_set_name
        description: Code-set name used to distinguish codes with the same value.
        data_tests: [not_null]
      - name: source_code_set_name
        description: NHS Data Model and Dictionary attribute name.
        data_tests: [not_null]
      - name: code
        description: National code, upper-cased and trimmed.
        data_tests: [not_null]
      - name: description
        description: Published code description.
      - name: source_effective_from_at
        description: Timestamp from which the UKHFD definition revision applies.

  - name: stg_ukhfd_data_dictionary_mhsds_referral_code_lookup
    description: >-
      UKHFD definitions for MHSDS referral code sets. One row per code set,
      code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - code_set_name
              - source_code_set_name
              - code
              - source_effective_from_at
    columns:
      - name: code_set_name
        description: Code-set name used to distinguish codes with the same value.
        data_tests: [not_null]
      - name: source_code_set_name
        description: NHS Data Model and Dictionary attribute name.
        data_tests: [not_null]
      - name: code
        description: National code, upper-cased and trimmed.
        data_tests: [not_null]
      - name: description
        description: Published code description.
      - name: source_effective_from_at
        description: Timestamp from which the UKHFD definition revision applies.

  - name: stg_ukhfd_data_dictionary_mhsds_service_or_team_intended_age_group
    description: >-
      UKHFD mental health service or team intended-age definitions. One row per
      code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_language_code
    description: UKHFD NHS language extensions. One row per code and definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_child_protection_plan_indicator
    description: UKHFD child protection plan indicators. One row per code and definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_looked_after_child_indicator
    description: UKHFD looked-after-child indicators. One row per code and definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_mental_health_admitted_patient_classification_type
    description: UKHFD mental health admitted patient classification types. One row per code and definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns


  - name: stg_ukhfd_data_dictionary_mhsds_inpatient_code_lookup
    description: >-
      UKHFD definitions for MHSDS inpatient code sets. One row per code set,
      source code set, code and UKHFD definition revision.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - code_set_name
              - source_code_set_name
              - code
              - source_effective_from_at
    columns:
      - name: code_set_name
        description: Code-set name used by the inpatient facts.
        data_tests: [not_null]
      - name: source_code_set_name
        description: NHS Data Model and Dictionary attribute name.
        data_tests: [not_null]
      - name: code
        description: National code, upper-cased and trimmed.
        data_tests: [not_null]
      - name: description
        description: Published code description.
      - name: source_effective_from_at
        description: Timestamp from which the UKHFD definition revision applies.

  - name: stg_ukhfd_data_dictionary_destination_of_discharge
    description: >-
      UKHFD discharge destination definitions. One row per code and definition
      revision. Retains all revisions and retired codes without filtering.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_discharge_destination_legacy
    description: >-
      UKHFD legacy discharge destination definitions. One row per code and definition
      revision. Retains all revisions and retired codes without filtering.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_method_of_discharge
    description: >-
      UKHFD discharge method definitions. One row per code and definition
      revision. Retains all revisions and retired codes without filtering.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns

  - name: stg_ukhfd_data_dictionary_discharge_method_legacy
    description: >-
      UKHFD legacy discharge method definitions. One row per code and definition
      revision. Retains all revisions and retired codes without filtering.
    config:
      meta:
        owner:
          name: Eddie Davison
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [code, source_effective_from_at]
    columns: *code_set_columns
