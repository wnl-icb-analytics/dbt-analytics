models:
  - name: practices_not_yet_in_olids_direct_care
    description: |
      Published view showing practices in the reference lookup with no registered
      patients in OLIDS demographics. One row per practice.

    config:
      meta:
        owner:
          name: EddieDavison92
    columns:
      - name: practice_code
        description: ODS code of the practice missing from OLIDS demographics.
        data_tests:
          - unique
